{
  "name": "toggle-menu",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "sass:watch": "node-sass ./scss/ --output ./css/ --output-style compressed --watch --source-map true"
  },
  "devDependencies": {
    "node-sass": "^5.0.0"
  }
}
